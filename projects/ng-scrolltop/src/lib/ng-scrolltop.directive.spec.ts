import { NgScrolltopDirective } from './ng-scrolltop.directive';

describe('NgScrolltopDirective', () => {
  it('should create an instance', () => {
    const directive = new NgScrolltopDirective();
    expect(directive).toBeTruthy();
  });
});
